
### 📋 Part 1: Foundation (Data Layer + State Changes)

```text
# Context: Hybrid Transition Points — Part 1 of 2 (Foundation)

งานนี้เตรียมโครงสร้างข้อมูลและ state สำหรับ NPC ข้ามโซนแบบเดินจริง
Part นี้ยังไม่เปลี่ยนพฤติกรรม NPC (ยังไม่ใช้ transition จริง) — แค่เตรียม data + state fields
Part 2 จะ implement logic การเดินผ่านจุดเชื่อมจริงๆ

# Architecture Constraints (เคร่งครัด)
1. **Luban CSV flat table**: ห้าม nested dictionary, ต้องมี header 3 บรรทัด (`##var`, `##type`, `##`)
2. **ใช้ `cfg.game.ZoneConnectionDef` ที่ Luban generate ตรงๆ**: ไม่ต้องสร้าง Shared mirror class
3. **`event.xml`**: เพิ่ม table declaration ตาม pattern เดียวกับ BiomeDef/HarvestableNodeDef
4. **MessagePack Keys**: NpcState ใช้ 0-15 อยู่แล้ว — field ใหม่เริ่มที่ [Key(16)]
5. **แก้ที่ `Shared/NpcState.cs` (root canonical)**: แล้วรัน `./sync-shared.sh` ห้ามแก้ copy ตรงๆ

# Task 1 — Data Layer: ZoneConnectionDef

## 1.1 สร้าง `DataTables/Data/ZoneConnectionDef.csv`
ตรวจ location id จริงจาก `LocationDef.csv` ก่อนเขียน (เช่น beach, jungle_edge, cave_entrance):

```csv
##var,id,fromLocationId,toLocationId,transitionX,transitionY
##type,string,string,string,float,float
##,connection id,from location,to location,transition x,transition y
,beach_to_jungle_edge,beach,jungle_edge,11,0
,jungle_edge_to_beach,jungle_edge,beach,9,0
,jungle_edge_to_deep_jungle,jungle_edge,deep_jungle,19,5
,deep_jungle_to_jungle_edge,deep_jungle,jungle_edge,21,5
,beach_to_cave_entrance,beach,cave_entrance,-4,4
,cave_entrance_to_beach,cave_entrance,beach,-6,6
```
(ปรับตัวเลขพิกัดให้สมเหตุสมผลกับ `WorldX/WorldY` จริง)

## 1.2 เพิ่ม table declaration ใน `DataTables/Defines/event.xml`
ตรวจ syntax ของ BiomeDef/HarvestableNodeDef ที่มีอยู่แล้ว แล้วเพิ่ม ZoneConnectionDef ตาม pattern เดียวกัน (field names camelCase ตรงกับ CSV)

## 1.3 รัน `DataTables/gen.sh` (หรือ `gen.bat`)
ยืนยันว่า:
- `Assets/Scripts/Data/Gen/game/ZoneConnectionDef.cs` ถูกสร้าง
- `TbZoneConnectionDef.cs` ถูกสร้าง
- `Tables.cs` มี `TbZoneConnectionDef` property
- JSON output ที่ `Assets/Resources/DataTables/game_tbzoneconnectiondef.json`

## 1.4 ปรับ `LubanDataService.cs`
```csharp
private Dictionary<(string from, string to), cfg.game.ZoneConnectionDef> _zoneConnectionsByPair = new();

// ใน LoadAll() หลังโหลดตารางอื่นเสร็จ:
_zoneConnectionsByPair = tables.TbZoneConnectionDef.DataList
    .ToDictionary(c => (c.FromLocationId, c.ToLocationId));

public cfg.game.ZoneConnectionDef GetTransition(string from, string to) =>
    _zoneConnectionsByPair.TryGetValue((from, to), out var def) ? def : null;
```

# Task 2 — NpcState.cs: เพิ่ม Ground Truth Fields

## 2.1 เพิ่ม enum (ในไฟล์เดียวกันหรือไฟล์แยก):
```csharp
public enum NpcTransitionPhase
{
    None,           // เดินอิสระปกติ
    WalkingToPoint, // กำลังเดินเข้าหาจุดเชื่อม
    Exiting,        // ถึงจุดเชื่อมแล้ว กำลังเล่น anim exit
    Entering,       // เพิ่งข้ามโซน กำลังเล่น anim enter
}
```

## 2.2 เพิ่ม fields ใน NpcState (ต่อจาก Key(15)):
```csharp
/// <summary>Ground truth — ห้ามหลุดเข้า NpcObservableView</summary>
[Key(16)] public NpcTransitionPhase TransitionPhase = NpcTransitionPhase.None;

/// <summary>Ground truth — โซนปลายทาง (ห้ามหลุดเข้า NpcObservableView)</summary>
[Key(17)] public string PendingTransitionTargetZoneId;
```

## 2.3 รัน `./sync-shared.sh`
ยืนยันว่าไฟล์ copy ใน `Marooned/Assets/Scripts/Shared/` และ `McpBridge/Shared/` อัปเดตตาม

# Output Requirements
- โค้ดครบทุกไฟล์ (CSV, event.xml diff, LubanDataService.cs, NpcState.cs)
- ยืนยัน Luban generation สำเร็จ (ไฟล์ .cs + .json ถูกสร้าง)
- ยืนยัน MessagePack Keys 16-17 ไม่ชนของเดิม (0-15)
- ยืนยัน TransitionPhase/PendingTransitionTargetZoneId ไม่หลุดเข้า NpcObservableView

**หมายเหตุ**: หลัง Part 1 เสร็จ NPC ยังไม่ใช้ transition จริง (ยัง teleport เหมือนเดิม) — Part 2 จะ implement logic การเดินผ่านจุดเชื่อม
```

---

### 📋 Part 2: Implementation (System + AI + View)

```text
# Context: Hybrid Transition Points — Part 2 of 2 (Implementation)

Part 1 เสร็จแล้ว: มี ZoneConnectionDef.csv + NpcState.TransitionPhase พร้อมใช้
Part นี้ implement logic การเดินผ่านจุดเชื่อมจริงๆ (Wander → NpcZoneTransitionSystem → View)

# Architecture Constraints
1. **Wander.cs เป็น single source of truth**: ทั้ง InnocentUtilityAI และ KillerPlanner เรียก helper เดียวกัน
2. **Plain C# ไม่มี Coroutine**: ใช้ `Dictionary<string, float>` นับถอยเวลา
3. **System ห้ามแตะ GameObject/View**: NpcZoneTransitionSystem เขียนแค่ ground truth fields → View ตรวจจับเอง
4. **Reuse `IChibiVisual.PlayAction(string)`**: ห้ามเพิ่ม interface method ใหม่
5. **MoveNpc() ยังเป็นจุดเดียวที่แก้ CurrentLocationId**: ใช้ escape hatch override ตำแหน่งหลังเรียก
6. **Tick Order**: NpcDirectorSystem → NpcMovementSystem → NpcZoneTransitionSystem

# Task 3 — Wander.cs: เดินผ่านจุดเชื่อม

## 3.1 เพิ่ม helper:
```csharp
public static bool IsTransitioning(NpcState npc) => npc.TransitionPhase != NpcTransitionPhase.None;
```

## 3.2 แก้ MoveToZone() และ MoveToRandomConnectedZone()
```csharp
public static bool MoveToZone(NpcState npc, UtilityContext ctx, string destinationId, Random rng, bool requireConnection = true)
{
    if (IsTransitioning(npc)) return false;
    if (string.IsNullOrEmpty(destinationId)) return false;
    if (!ctx.Data.LocationDefs.TryGetValue(destinationId, out var destinationDef)) return false;

    if (destinationId == npc.CurrentLocationId)
        return SetRandomTargetInZone(npc, ctx, rng);

    if (requireConnection)
    {
        if (!ctx.Data.LocationDefs.TryGetValue(npc.CurrentLocationId, out var currentDef)) return false;
        var connections = currentDef.ConnectedLocationIds;
        if (connections == null || !connections.Contains(destinationId)) return false;
    }

    var transition = ctx.Data.GetTransition(npc.CurrentLocationId, destinationId);
    if (transition == null)
    {
        ctx.NpcDirector.MoveNpc(npc.Id, destinationDef.Id); // fallback teleport
        return true;
    }

    npc.TargetX = transition.TransitionX;
    npc.TargetY = transition.TransitionY;
    npc.Activity = NpcActivityState.Traveling;
    npc.TransitionPhase = NpcTransitionPhase.WalkingToPoint;
    npc.PendingTransitionTargetZoneId = destinationId;
    return true;
}
```

# Task 4 — NpcZoneTransitionSystem.cs (ใหม่)

```csharp
namespace Marooned.Systems
{
    public class NpcZoneTransitionSystem
    {
        public float ExitAnimSeconds = 0.5f;
        public float EnterAnimSeconds = 0.3f;
        public float ArrivalThreshold = 0.1f;

        private readonly NpcDirectorSystem _npcDirector;
        private readonly LubanDataService _data;
        private readonly Dictionary<string, float> _timers = new();

        public NpcZoneTransitionSystem(NpcDirectorSystem npcDirector, LubanDataService data)
        {
            _npcDirector = npcDirector;
            _data = data;
        }

        public void Tick(float deltaSeconds)
        {
            foreach (var npc in _npcDirector.Npcs.Values)
            {
                if (!npc.IsAlive)
                {
                    if (npc.TransitionPhase != NpcTransitionPhase.None)
                    {
                        npc.TransitionPhase = NpcTransitionPhase.None;
                        npc.PendingTransitionTargetZoneId = null;
                        _timers.Remove(npc.Id);
                    }
                    continue;
                }

                switch (npc.TransitionPhase)
                {
                    case NpcTransitionPhase.WalkingToPoint: TickWalkingToPoint(npc); break;
                    case NpcTransitionPhase.Exiting: TickTimer(npc, deltaSeconds, CompleteExit); break;
                    case NpcTransitionPhase.Entering: TickTimer(npc, deltaSeconds, CompleteEnter); break;
                }
            }
        }

        private void TickWalkingToPoint(NpcState npc)
        {
            var dx = npc.TargetX - npc.PositionX;
            var dy = npc.TargetY - npc.PositionY;
            if (dx * dx + dy * dy > ArrivalThreshold * ArrivalThreshold) return;

            npc.TransitionPhase = NpcTransitionPhase.Exiting;
            _timers[npc.Id] = ExitAnimSeconds;
        }

        private void TickTimer(NpcState npc, float deltaSeconds, Action<NpcState> onComplete)
        {
            if (!_timers.TryGetValue(npc.Id, out var remaining)) { onComplete(npc); return; }
            remaining -= deltaSeconds;
            if (remaining <= 0f) { _timers.Remove(npc.Id); onComplete(npc); }
            else _timers[npc.Id] = remaining;
        }

        private void CompleteExit(NpcState npc)
        {
            var targetZone = npc.PendingTransitionTargetZoneId;
            var previousZone = npc.CurrentLocationId;

            _npcDirector.MoveNpc(npc.Id, targetZone);

            var arrival = _data.GetTransition(targetZone, previousZone);
            if (arrival != null)
            {
                npc.PositionX = arrival.TransitionX;
                npc.PositionY = arrival.TransitionY;
                npc.TargetX = npc.PositionX;
                npc.TargetY = npc.PositionY;
            }

            npc.TransitionPhase = NpcTransitionPhase.Entering;
            npc.PendingTransitionTargetZoneId = null;
            _timers[npc.Id] = EnterAnimSeconds;
        }

        private void CompleteEnter(NpcState npc)
        {
            npc.TransitionPhase = NpcTransitionPhase.None;
        }
    }
}
```

# Task 5 — AI Integration

## 5.1 InnocentUtilityAI.Tick()
```csharp
public void Tick(NpcState npc, float deltaSeconds)
{
    if (Wander.IsTransitioning(npc)) return;
    // ... logic เดิม
}
```

## 5.2 KillerPlanner.Tick()
```csharp
public void Tick(NpcState killer, float deltaSeconds)
{
    if (Wander.IsTransitioning(killer)) return;
    // ... logic เดิม
}
```

# Task 6 — NpcCharacterView.cs

เพิ่ม logic ตรวจจับ TransitionPhase เปลี่ยน:
```csharp
private NpcTransitionPhase _lastPhase = NpcTransitionPhase.None;

private void Update()
{
    if (string.IsNullOrEmpty(_npcId) || _npcDirector == null) return;
    if (!_npcDirector.Npcs.TryGetValue(_npcId, out var npc)) return;

    if (_visual == null) _visual = GetComponentInChildren<IChibiVisual>();

    transform.position = new Vector3(npc.PositionX, npc.PositionY, 0f);

    if (npc.TransitionPhase != _lastPhase)
    {
        if (npc.TransitionPhase == NpcTransitionPhase.Exiting) _visual?.PlayAction("zone_exit");
        else if (npc.TransitionPhase == NpcTransitionPhase.Entering) _visual?.PlayAction("zone_enter");
        _lastPhase = npc.TransitionPhase;
    }

    if (npc.TransitionPhase == NpcTransitionPhase.Exiting || npc.TransitionPhase == NpcTransitionPhase.Entering)
        return;

    // facing + Bind ปกติ (logic เดิม)
    var dx = npc.TargetX - npc.PositionX;
    if (Mathf.Abs(dx) > 0.02f)
    {
        var facingRight = dx > 0f;
        if (!_facingInitialized || facingRight != _lastFacingRight)
        {
            _visual?.SetFacing(facingRight);
            _lastFacingRight = facingRight;
            _facingInitialized = true;
        }
    }
    _visual?.Bind(npc.Activity);
}
```

# Task 7 — DI + Tick Order

## 7.1 GameLifetimeScope.Configure()
```csharp
builder.Register<NpcZoneTransitionSystem>(Lifetime.Singleton).AsSelf();
```

## 7.2 GameTickDriver.cs
```csharp
_npcDirector.Tick(deltaSeconds);
_npcMovement.Tick(deltaSeconds);
_npcZoneTransition.Tick(deltaSeconds); // หลัง Movement เสมอ
```

# Task 8 — Runtime Tests

- **A**: NPC เดินเห็นจริงไปยังจุดเชื่อมโซน
- **B**: ถึงจุดเชื่อม → หยุด ~0.5 วิ → ข้ามโซน → โผล่ที่ arrival point → หยุด ~0.3 วิ → เดินต่อ
- **C**: log ยืนยัน AI ไม่ตั้ง target ซ้อนระหว่าง transition
- **D**: คู่ที่ไม่มีใน CSV → fallback teleport ไม่ error
- **E**: NPC ตายกลาง transition → cleanup ถูกต้อง
- **F**: Regression — Player/get_visible_npcs/get_game_state ไม่เปลี่ยน

# Output
- โค้ดครบทุกไฟล์ (Wander.cs, NpcZoneTransitionSystem.cs ใหม่, InnocentUtilityAI.cs, KillerPlanner.cs, NpcCharacterView.cs, GameLifetimeScope.cs, GameTickDriver.cs)
- หลักฐานเทส A-F
```
-----------------------------------------------------------------------------------------------------------


---

### 📋 Prompt Round 2.5A (Corrected v2)

```text
# Context: Phase 2.5A — Living NPCs (Survival Tick + Motive Events + Stub Actions)

NPC เดินได้แล้ว (Traveling), Killer วางแผนแล้ว แต่ stats นิ่งสนิท ไม่เคย tick
Fear/Curiosity ไม่เคยเปลี่ยน Stub actions เป็น dead code
รอบนี้ทำให้ NPC "รู้สึก" ถึงโลกจริง

# ⚠️ กฎเหล็ก — ต้องทำก่อนเขียนโค้ดทุกบรรทัด

1. **ตรวจโค้ดจริงก่อนเขียนเสมอ ห้ามเชื่อเลขใน prompt นี้**:
   - อ่าน `Shared/NpcState.cs` ดูก่อนว่าใช้ Key ถึงตัวไหนแล้ว (เคยไปถึง 15 หรือ 17 แล้ว)
   - อ่าน `Shared/NpcSurvivalState.cs` ดูก่อนว่ามี field อะไรบ้าง, สเกล 0-100 หรือ 0-1
   - ถ้า Hybrid Transition Points ถูกทำไปแล้ว ต้องรู้จัก `NpcTransitionPhase` ด้วย
2. **ยึดสเกล 0-100 ของที่มีอยู่แล้ว** (คอมเมนต์ในโค้ดเขียนไว้ชัด) — ห้ามใช้ 0-1
3. **ห้ามสร้างกลไก finished/state พิเศษใหม่** — ใช้ decay + re-evaluate ธรรมชาติของ utility AI
4. **ห้ามแก้ serialized references บริเวณ ChibiSpawnerView ใน scene YAML**

# Architecture Constraints
- Tick Order: NpcSurvivalSystem → NpcDirectorSystem → NpcMovementSystem → NpcZoneTransition
- DI: Register เป็น Lifetime.Singleton ใน GameLifetimeScope.Configure()
- Disposable: implement `IDisposable` (pattern เดียวกับ `WorldItemSystem`) — subscribe ใน constructor
  เก็บ `IDisposable` ไว้ dispose ตอน container ปิด
- MessagePipe: subscribe NpcEliminatedMessage (discrete event ถูกที่)
- Information Hiding: stats = ground truth ห้ามหลุดเข้า observables/MCP responses

# Task

## Step 1: NpcSurvivalSystem.cs (Plain C# Singleton)
- **ตรวจก่อน**: ถ้า `NpcSurvivalState` มีแค่ `Hunger`, `Fear`, `Curiosity` (Key 0-2) —
  ไม่ต้องเพิ่ม field ใหม่ ใช้เท่าที่มี (Thirst/Fatigue ของ NPC ไม่จำเป็นรอบนี้
  เพราะมีแต่ Player ที่มีครบ 4 stat)
- **ถ้าต้องการเพิ่มจริง**: เพิ่มในคลาสย่อย `NpcSurvivalState` เองที่ Key 3-4
  (คนละ numbering space กับ NpcState หลัก) — ไม่ใช่ใน NpcState
- **Tick(dt) ต่อ NPC ที่ IsAlive** (ใช้สเกล 0-100 ตามที่มีอยู่แล้ว):
  - `Hunger += 2f * dt` (clamp 0..100) — หิวช้าๆ
  - `Fear -= 6f * dt` (clamp 0..100) — decay กลับ baseline ~17 วิ
  - `Curiosity -= 3f * dt` (clamp 0..100) — decay กลับ baseline ~33 วิ
- Constants ชัดเจน + TODO ย้ายไป Luban ทีหลัง

## Step 2: Motive Event Hooks (ผ่าน MessagePipe subscribe)
- Subscribe `NpcEliminatedMessage` (เก็บ `IDisposable` ไว้):
  - NPC ที่ location เดียวกับเหยื่อ: `Survival.Fear += 50f` (clamp 0..100) — "เห็นศพ"
  - NPC ที่ connected location: `Survival.Curiosity += 30f` (clamp 0..100) — "ได้ยินเสียง"

## Step 3: เปิด Stub Actions (ใช้ pattern SeekFoodAction เดิม)
- **FleeToSafeZoneAction**:
  ```csharp
  Score(npc, ctx) => npc.Survival.Fear / 100f;   // normalize 0-1 เหมือน SeekFoodAction
  Execute(npc, ctx, dt) => เรียก Wander.MoveToRandomConnectedZone เท่านั้น
    ห้ามเขียน movement/teleport logic เอง; return false (ไม่ finished —
    ปล่อยให้ decay ทำให้ Fear ตกลงในรอบ re-evaluate ถัดไป แล้ว IdleWander จะชนะเอง)
  ```
- **InvestigateNoiseAction**:
  ```csharp
  Score(npc, ctx) => (npc.Survival.Curiosity / 100f) * (1f - npc.Survival.Fear / 100f);
    // Curiosity=80, Fear=30 → 0.8 * 0.7 = 0.56 (ถูกต้อง ไม่ใช่ -2320 แบบสเกล 0-100 โดยตรง)
  Execute(npc, ctx, dt) => เรียก Wander.MoveToZone(targetLocationId) เท่านั้น
    // ห้ามเพิ่ม timer "หยุดดู N วิ" — ขัดกับกฎ stateless ของ IUtilityAction
    // ให้ NpcMovementSystem idle ธรรมชาติเมื่อถึงจุด แล้ว decay พาไปยัง action อื่น
    return false;
  ```

## Step 4: Wander Clamp (ระวังชน Transition Points)
- **ตรวจก่อน**: มี `NpcTransitionPhase` หรือยัง (Hybrid Transition Points)
- **ถ้ามี**: clamp `PositionX/Y` และ `TargetX/Y` เฉพาะตอน `npc.TransitionPhase == None`
  — ถ้ากำลังข้ามโซน (TransitionPhase != None) ห้าม clamp เพราะจุดเชื่อมโซน
  อาจอยู่นอกรัศมี wander (WorldX ของ beach=0, wander radius ~2.5, transition x=11)
- **ถ้ายังไม่มี**: clamp ทุกกรณีในรัศมี wander รอบ `LocationDef.WorldX/Y`

## Step 5: Runtime Tests
- A: Hunger เพิ่มตามเวลา (log ทุก 30 วิเกม), Fear/Curiosity decay กลับ baseline
- B: force kill → ผู้รอดชีวิตในโซนเดียวกัน Fear พุ่งถึง 50+ → FleeToSafeZone ชนะ re-evaluate
- C: NPC ที่ Curiosity สูงเดินไปสำรวจจุดพบศพ
- D: ไม่มี NPC ตัวไหนหลุดออกนอกรัศมีโซน (หรือข้ามโซนได้ปกติ ถ้าทำ transition แล้ว)
- E: regression — info hiding diff MCP responses ก่อน-หลัง ต้องไม่ leak stats

# Output: ไฟล์ที่แก้, ตัวอย่าง log motive spike, หลักฐานเทส A–E, ยืนยันการตรวจโค้ดจริงก่อนเขียน
```

**Commit:** `Unity-v0.0.11-feat(NpcAI): living NPCs — survival tick + fear/curiosity events + flee & investigate`

---

-----------------------------------------------------------------------------------------------------------
Task 1: AI Query Methods (สำหรับ Debug)
  - InnocentUtilityAI.GetCurrentActionId(npcId)
  - KillerPlanner.GetCurrentPhase(npcId)

Task 2: NpcDebugOverlay (MonoBehaviour)
  - #if UNITY_EDITOR guard, toggle F12
  - Resolve NpcDirectorSystem / InnocentUtilityAI / KillerPlanner จาก GameLifetimeScope
  - แสดง: Id | Role | Loc | Pos | Act | Hunger (อ่านจาก npc.Survival.Hunger — 
    ไม่ต้อง implement tick logic เอง เพราะ NpcSurvivalSystem ทำให้แล้ว) | AI action/phase

Task 3: Setup & Verification
  - Test A: F12 เปิด/ปิด overlay ได้
  - Test B: Hunger ที่โชว์ขยับตามเวลาจริง (verify ผ่าน NpcSurvivalSystem ที่มาจาก Doc 254 
    ไม่ใช่ implement ใหม่)
  - Test C: Hunger > 55 → AI column เปลี่ยนเป็น seek_food
  - Test D: build target อื่นต้องไม่มี error (#if UNITY_EDITOR ทำงานถูก)

  -----------------------------------------------------------------------------------------------------------
  