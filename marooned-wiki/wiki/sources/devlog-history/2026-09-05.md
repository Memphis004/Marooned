---
title: 2026-09-05
type: devlog
sources: []
related:
  - "[[overview]]"
  - "[[conventions]]"
  - "[[mcp-tool-table]]"
folder: devlog-history
created: 2026-09-05
tags:
  - devlog
  - lab-a
  - marooned
---

# Dev Log — 2026-09-05

## 📌 สรุปวัน
วันแรกของการพัฒนาโปรเจค Marooned (Card Survival × Social Deduction) — เปลี่ยนจาก
"code scaffold เงียบ ๆ" เป็น "โปรเจคที่มีเอกสารครบและตรวจสอบได้" พร้อมปิดช่องว่างระหว่าง
สิ่งที่ GDD บอกกับสิ่งที่โค้ดทำจริง

## ✅ สิ่งที่ทำได้วันนี้

### 1. Lab A Scaffold (มีอยู่ก่อนหน้า ตรวจสอบแล้ว)
- โครงสร้างโปรเจคครบ: Systems/ (8), UI/ (10), Core/ (1), Data/ (+ Luban Gen 13)
- Shared types 7 ไฟล์ + `sync-shared.sh` ใช้งานได้ (ตรวจแล้ว copy ตรงทั้ง 2 ปลายทาง)
- McpBridge: **implement ครบ 10 tools แล้ว** (ตรวจพบว่าไม่มี `NotImplementedException`
  เหลืออยู่ — สถานะเก่าใน AGENTS.md ล้าสมัย)
- Luban pipeline ผ่านการ generate จริง (มี JSON output ใน `Assets/Resources/DataTables/`)
  แต่ `LubanDataService` ยังใช้ mock

### 2. Karpathy LLM Wiki Setup
- สร้าง Obsidian Vault: `marooned-wiki/`
- ติดตั้ง Karpathy LLM Wiki Plugin + Smart Connections
- เชื่อมต่อ Ollama (Local LLM) สำหรับ query wiki
- จัดตำแหน่ง GDD เข้าที่ `wiki/sources/game-design-doc/` (canonical ตาม AGENTS.md)

### 3. ZCode Integration (เอกสารจากโค้ดจริง)
- ตั้งค่า ZCode (GLM-5.3) + AGENTS.md (wiki-first workflow, port 3216)
- ให้ ZCode อ่านโค้ดทุกไฟล์แล้ว generate เอกสาร 3 หมวด:
  - **Architecture:** `overview.md` (2,623 บรรทัด — ฝังโค้ดต้นฉบับครบ),
    `mcp-bridge.md`, `mcp-tool-table.md`
  - **Code Snippets:** 34 ไฟล์ ครบทุก .cs ใน Systems/Core/Data/UI
    (fronmatter + โค้ดต้นฉบับ + Purpose/Public API/Dependencies/Key Logic/TODO)
  - **Mechanics:** 8 ไฟล์ (survival-stats, card-inventory, crafting, exploration,
    npc-director, deduction, world-events, chibi-avatar)

### 4. Documentation
- สร้าง `index.md` (สารบัญรวมทุกเอกสาร)
- สร้าง `conventions.md` (coding conventions จากโค้ดจริง)
- สร้าง devlog นี้

### 5. ข้อค้นพบสำคัญจากการ audit โค้ด (บันทึกไว้ในเอกสารแล้ว)
- **ไม่มี game loop เรียก `Tick()`** — SurvivalStatSystem/NpcDirectorSystem/
  WorldEventSystem ยังหยุดนิ่งจนกว่าจะมี driver
- `AwaitNextEvent` ยังเป็น naive poll (ไม่ block, ไม่เคารพ `TimeoutSeconds`)
- `PlayerSurvivalState.CollectedClueCardIds` ไม่เคยถูกเติม → `GetClueBoard` ตอบว่างเสมอ
- `ExplorationSystem.Explore` มี side effect ย้ายตำแหน่งผู้เล่น
- `GameLifetimeScope` ยังต้อง verify API ของ MessagePipe.Interprocess ใน Unity จริง

## ⚠️ ปัญหาที่เจอและวิธีแก้
- **YAML Frontmatter syntax error:** ZCode เขียน `related: [[A]], [[B]]` และ
  `tags: [[x]]` บรรทัดเดียว — Obsidian parse ไม่ได้ (flow sequence ซ้ำในบรรทัดเดียว)
  → แก้ทั้ง 34 ไฟล์เป็น YAML list ขึ้นบรรทัดใหม่ (`related:` ใช้ quoted `"[[...]]"`
  `tags:` ใช้ plain text) และบังคับกฎใน prompt ครั้งต่อไป
- **Obsidian frontmatter เสียหายจากการแก้มือ:** 2 ไฟล์ (CardHandPresenter/CardHandView)
  มี item ซ้ำ/ย่อหน้าเพี้ยน → rebuild frontmatter ให้สะอาดและ validate ด้วย
  `yaml.safe_load` ทั้ง folder
- **GDD ถูกย้ายตำแหน่ง:** อ้าง path เก่า `marooned-wiki/game-design-doc/` → แก้ทุก
  reference เป็น `marooned-wiki/wiki/sources/game-design-doc/`
- **AGENTS.md ล้าสมัย:** บอกว่า McpBridge tools เป็น NotImplementedException ทั้งหมด
  (ไม่จริงแล้ว) — ควรอัปเดต AGENTS.md ในครั้งหน้า

## 🎯 TODO — วันถัดไป
- [ ] เพิ่ม **game loop/tick driver** เรียก `Tick()` ของ 3 systems (สิ่งแรกที่ควรทำ —
  ไม่งั้นเกมนิ่งสนิท)
- [ ] ทดสอบ MCP Bridge round trip จริง (Unity Play → `dotnet run` → `GetGameState`)
  และ verify API ของ `GameLifetimeScope`
- [ ] Wire Luban pipeline จริง: เขียน loader + mapper `cfg.game.*` → `Marooned.Shared.*`
  แทน mock ใน `LubanDataService` (TODO Lab A+1)
- [ ] เพิ่มกลไกเก็บ clue เข้า `CollectedClueCardIds` ให้ `GetClueBoard` มีข้อมูล
- [ ] เริ่มสร้าง Unity scenes + prefabs เบื้องต้น (CoreScene HUD)
- [ ] ทดสอบ AI VTuber เรียก MCP tools ครบ 10 ตัว
- [ ] อัปเดต AGENTS.md (สถานะ McpBridge, ตำแหน่ง GDD)

## 📊 สถานะโปรเจค
- **Lab A Target:** Code scaffold ✅ → Playable build ❌ (ขาด game loop + scene)
- **Wiki Coverage:** architecture/mechanics/code-snippets ครบทุกไฟล์ที่มีโค้ดจริง
- **MCP Tools:** 10/10 implemented ใน Bridge (⚠️ logic ฝั่ง Unity บางตัวเป็น placeholder —
  รายละเอียดใน [[mcp-tool-table]])
- **เอกสาร:** 46 ไฟล์ (1 overview + 2 mcp-architecture + 34 snippets + 8 mechanics +
  index/conventions/devlog)
