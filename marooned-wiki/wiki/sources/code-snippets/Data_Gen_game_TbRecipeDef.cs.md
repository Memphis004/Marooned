---
title: TbRecipeDef
type: snippet
sources: ["[[sources/tbrecipedef-cs]]"]
related:
  - Luban
  - "[[LubanDataService.cs]]"
  - "[[Data_Gen_Tables.cs]]"
folder: Data/Gen
lines: 54
created: 2026-09-05
tags:
  - Data/Gen
  - marooned
  - lab-a
---

# TbRecipeDef.cs
**Path:** `Marooned/Assets/Scripts/Data/Gen/game/TbRecipeDef.cs` (54 lines)

## Source
```csharp

//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using Luban;
using Luban.SimpleJSON;


namespace cfg.game
{
public partial class TbRecipeDef
{
    private readonly System.Collections.Generic.Dictionary<string, game.RecipeDef> _dataMap;
    private readonly System.Collections.Generic.List<game.RecipeDef> _dataList;
    
    public TbRecipeDef(JSONNode _buf)
    {
        int count = _buf.Count;
        _dataMap = new System.Collections.Generic.Dictionary<string, game.RecipeDef>(count);
        _dataList = new System.Collections.Generic.List<game.RecipeDef>(count);
        
        foreach(JSONNode _ele in _buf.Children)
        {
            game.RecipeDef _v;
            { if(!_ele.IsObject) { throw new SerializationException(); }  _v = global::cfg.game.RecipeDef.DeserializeRecipeDef(_ele);  }
            _dataList.Add(_v);
            _dataMap.Add(_v.Id, _v);
        }
    }

    public System.Collections.Generic.IReadOnlyDictionary<string, game.RecipeDef> DataMap => _dataMap;
    public System.Collections.Generic.IReadOnlyList<game.RecipeDef> DataList => _dataList;

    public game.RecipeDef GetOrDefault(string key) => _dataMap.TryGetValue(key, out var v) ? v : default;
    public game.RecipeDef Get(string key) => _dataMap[key];
    public game.RecipeDef this[string key] => _dataMap[key];

    public void ResolveRef(Tables tables)
    {
        foreach(var _v in _dataList)
        {
            _v.ResolveRef(tables);
        }
    }

}

}

```

# TbRecipeDef

## Purpose
Luban auto-generated table wrapper สำหรับตาราง `TbRecipeDef` — โหลด array จาก JSON ทั้งไฟล์แล้ว
index เป็น Dictionary ตาม primary key (key = recipe id) **ห้ามแก้มือ**

## Public API
| Member | คำอธิบาย |
| --- | --- |
| `TbRecipeDef(JSONNode _buf)` | Constructor — วน JSON array, deserialize เป็น bean, เติม `_dataList` + `_dataMap` |
| `IReadOnlyDictionary<string, game.RecipeDef> DataMap { get; }` | ทั้งตารางแบบ keyed ด้วย `Id` |
| `IReadOnlyList<game.RecipeDef> DataList { get; }` | ทั้งตารางตามลำดับในไฟล์ |
| `game.RecipeDef GetOrDefault(string key)` | คืน row หรือ null ถ้าไม่เจอ |
| `game.RecipeDef Get(string key)` | คืน row — throw ถ้าไม่มี key |
| `game.RecipeDef this[string key]` | Indexer = `Get` |
| `void ResolveRef(Tables tables)` | เรียก ResolveRef ของทุก row |

## Dependencies
- Luban runtime + bean class `game.RecipeDef` (ไฟล์ `game/RecipeDef.cs`)
- `Tables` — สร้าง instance นี้ด้วย `loader("game_tbTbRecipeDef")`

## Key Logic
Constructor วนทุก element ของ JSON array → deserialize เป็น bean → เพิ่มทั้ง list (ลำดับ) และ
map (index ด้วย `Id`) — เป็น read-only snapshot ของตาราง

## TODO / Known Issues
- Auto-generated — regenerate ผ่าน `DataTables/gen.sh` เท่านั้น
- **ยังไม่มีใครอ่าน JSON ที่เกี่ยวข้อง** (`Marooned/Assets/Resources/DataTables/game_tbTbRecipeDef.json`)
  — [[LubanDataService.cs]] ยังใช้ mock (TODO Lab A+1)
