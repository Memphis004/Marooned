---
title: TbCardDef
type: snippet
sources: [Marooned/Assets/Scripts/Data/Gen/game/TbCardDef.cs]
related:
  - Luban
  - "[[LubanDataService.cs]]"
  - "[[Data_Gen_Tables.cs]]"
folder: Data/Gen
lines: 54
created: 2026-09-05
tags:
  - Data/Gen
  - marooned
  - lab-a
---

# TbCardDef.cs
**Path:** `Marooned/Assets/Scripts/Data/Gen/game/TbCardDef.cs` (54 lines)

## Source
```csharp

//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using Luban;
using Luban.SimpleJSON;


namespace cfg.game
{
public partial class TbCardDef
{
    private readonly System.Collections.Generic.Dictionary<string, game.CardDef> _dataMap;
    private readonly System.Collections.Generic.List<game.CardDef> _dataList;
    
    public TbCardDef(JSONNode _buf)
    {
        int count = _buf.Count;
        _dataMap = new System.Collections.Generic.Dictionary<string, game.CardDef>(count);
        _dataList = new System.Collections.Generic.List<game.CardDef>(count);
        
        foreach(JSONNode _ele in _buf.Children)
        {
            game.CardDef _v;
            { if(!_ele.IsObject) { throw new SerializationException(); }  _v = global::cfg.game.CardDef.DeserializeCardDef(_ele);  }
            _dataList.Add(_v);
            _dataMap.Add(_v.Id, _v);
        }
    }

    public System.Collections.Generic.IReadOnlyDictionary<string, game.CardDef> DataMap => _dataMap;
    public System.Collections.Generic.IReadOnlyList<game.CardDef> DataList => _dataList;

    public game.CardDef GetOrDefault(string key) => _dataMap.TryGetValue(key, out var v) ? v : default;
    public game.CardDef Get(string key) => _dataMap[key];
    public game.CardDef this[string key] => _dataMap[key];

    public void ResolveRef(Tables tables)
    {
        foreach(var _v in _dataList)
        {
            _v.ResolveRef(tables);
        }
    }

}

}

```

# TbCardDef

## Purpose
Luban auto-generated table wrapper สำหรับตาราง `TbCardDef` — โหลด array จาก JSON ทั้งไฟล์แล้ว
index เป็น Dictionary ตาม primary key (key = card id) **ห้ามแก้มือ**

## Public API
| Member | คำอธิบาย |
| --- | --- |
| `TbCardDef(JSONNode _buf)` | Constructor — วน JSON array, deserialize เป็น bean, เติม `_dataList` + `_dataMap` |
| `IReadOnlyDictionary<string, game.CardDef> DataMap { get; }` | ทั้งตารางแบบ keyed ด้วย `Id` |
| `IReadOnlyList<game.CardDef> DataList { get; }` | ทั้งตารางตามลำดับในไฟล์ |
| `game.CardDef GetOrDefault(string key)` | คืน row หรือ null ถ้าไม่เจอ |
| `game.CardDef Get(string key)` | คืน row — throw ถ้าไม่มี key |
| `game.CardDef this[string key]` | Indexer = `Get` |
| `void ResolveRef(Tables tables)` | เรียก ResolveRef ของทุก row |

## Dependencies
- Luban runtime + bean class `game.CardDef` (ไฟล์ `game/CardDef.cs`)
- `Tables` — สร้าง instance นี้ด้วย `loader("game_tbTbCardDef")`

## Key Logic
Constructor วนทุก element ของ JSON array → deserialize เป็น bean → เพิ่มทั้ง list (ลำดับ) และ
map (index ด้วย `Id`) — เป็น read-only snapshot ของตาราง

## TODO / Known Issues
- Auto-generated — regenerate ผ่าน `DataTables/gen.sh` เท่านั้น
- **ยังไม่มีใครอ่าน JSON ที่เกี่ยวข้อง** (`Marooned/Assets/Resources/DataTables/game_tbTbCardDef.json`)
  — [[LubanDataService.cs]] ยังใช้ mock (TODO Lab A+1)
